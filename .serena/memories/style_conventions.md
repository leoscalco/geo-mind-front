Codebase conventions: strict TypeScript, path aliases via @/*, App Router structure, mostly functional React components, explicit DTO interfaces in types/geoMind.ts, API helpers in lib/api.ts, Tailwind utility classes for styling, Radix/Lucide/Recharts for UI. ESLint uses eslint-config-next/core-web-vitals and typescript presets. Important repo-specific rule from AGENTS.md: this project uses a non-trivial Next.js 16 setup; before editing code, consult the relevant docs under node_modules/next/dist/docs/ rather than relying on stale framework assumptions.